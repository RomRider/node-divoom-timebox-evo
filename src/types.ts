export enum ChannelType {
  Time,
  Lightning,
  Cloud,
  VJEffects,
  Visualization,
  Animation,
  Scoreboard
}

export enum TimeDisplayType {
  FullScreen,
  Rainbow,
  WithBox,
  AnalogSquare,
  FullScreenNegative,
  AnalogRound
}

export enum LightningType {
  PlainColor,
  Love,
  Plants,
  NoMosquitto,
  Sleeping
}
export enum WeatherType {
  Clear = 1,
  CloudySky = 3,
  Thunderstorm = 5,
  Rain = 6,
  Snow = 8,
  Fog = 9
}
